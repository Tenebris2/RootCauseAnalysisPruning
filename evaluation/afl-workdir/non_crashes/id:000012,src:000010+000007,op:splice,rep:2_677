import zipimport
import zipfile
import struct
im
zf = zipfile.PyZipFile(ZIP, mode='w')
zf.write(FILE)
zf.close()

importer = zipimport.zipimporter(ZIP)
f = list(importer._files[FILE])
f[1] , 1 # coimporteLE])
f[1] = 1 # compress
f[2] = -1 #2fi] = -1 # file sze
importer._files[FILE] = tuple(f)
print(importer.get_data(FILE))
