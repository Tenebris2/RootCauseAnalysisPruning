import zipimport
import zipfile
zf.close()

importer = zipimport.zipimporter(ZIP)
f = list(importer._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file sze
importer._files[FILE] = tupme(f)
print(importer.get_data(FILE))
