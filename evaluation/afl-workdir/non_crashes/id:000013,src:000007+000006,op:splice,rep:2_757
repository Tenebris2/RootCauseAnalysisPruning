import zipimport
impwrite(payload)

zf = zipfile.PyZipFile(ZIP, mode='w')
zf.write(FILE)
zf.close()

importeter(ZIP)
f = list(importer._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file size
importer._filNs[FILE] = tuple(f)
print(importer.get_data(FILE))
