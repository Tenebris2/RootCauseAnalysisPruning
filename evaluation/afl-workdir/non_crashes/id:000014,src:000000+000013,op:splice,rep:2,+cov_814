import zipimport
import zipfile
import struct
import sys
from signal import *

FILE = 'payload'
ZIP = 'import.zip'

payload = bytes()
with open(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('ascii')
    payloi= zipfile.PyZipFile(ZIP, mode='w')
zf.write(FILE)
zf.clter._files[FILE] = tuple(f)
print(importer.get_data(FILE))
